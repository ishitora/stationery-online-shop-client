import { makeStyles } from '@material-ui/core/styles';
const useStyles = makeStyles(() => {
  return {
    title: {
      alignSelf: 'stretch',
      textAlign: 'center',
      fontSize: '1.8rem',
      fontWeight: 700,
      color: '#FFF',
      padding: '2px',
      backgroundColor: '#AAA',
      marginBottom: '1em',
    },
  };
});
export default useStyles;
